{
  "name": "convex",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ 0xsign/convex",
    "create-local": "graph create --node http://localhost:8020/ 0xsign/convex",
    "remove-local": "graph remove --node http://localhost:8020/ 0xsign/convex",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xsign/convex"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
